from os.path import join


root = ""

# All paths are defined relative to repo root
data_dir = join(root, "data")
train_dir = join(data_dir, "train")
val_dir = join(data_dir, "val")
sequences_dir = join(data_dir, "sequences-raw")

